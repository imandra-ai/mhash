
(library
  (name ppx_deriving_hash)
  (kind ppx_deriver)
  (ppx_runtime_libraries ppx_deriving_hash.runtime)
  (libraries ppxlib)
  (preprocess (pps ppxlib.metaquot))
  (flags :standard -warn-error -a+8))
