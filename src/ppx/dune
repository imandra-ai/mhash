
(library
  (name ppx_deriving_mhash)
  (public_name ppx_deriving_mhash)
  (kind ppx_deriver)
  (ppx_runtime_libraries mhash)
  (libraries ppxlib)
  (preprocess (pps ppxlib.metaquot))
  (flags :standard -warn-error -a+8))
